{
  "name": "sarthi-ai-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Sarthi AI (BharatAI) Workspace - Root Launcher",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && venv\\Scripts\\python.exe -m uvicorn main:app --reload --port 8000",
    "dev:node": "cd backend/node && npm run dev"
  }
}
