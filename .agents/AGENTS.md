# Project Rules for Sarthi AI

## Git Operations & GitHub Push Constraint
- **NEVER automatically push changes to GitHub (`git push`).**
- Always complete code changes locally, test them, and explicitly ask the user for permission before running `git push`.
- Only execute `git push` when the user explicitly requests or confirms "push to github".
